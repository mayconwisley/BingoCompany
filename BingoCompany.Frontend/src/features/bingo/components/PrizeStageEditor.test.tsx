import { fireEvent,render,screen } from "@testing-library/react";import { describe,expect,it,vi } from "vitest";import { PrizeStageEditor } from "./PrizeStageEditor";
describe("PrizeStageEditor",()=>{it("adiciona uma etapa de prêmio",()=>{const onChange=vi.fn();render(<PrizeStageEditor stages={[{sequence:1,prizeName:"TV",pattern:"FullCard"}]} onChange={onChange}/>);fireEvent.click(screen.getByRole("button",{name:"+ Adicionar prêmio"}));expect(onChange).toHaveBeenCalledWith([{sequence:1,prizeName:"TV",pattern:"FullCard"},{sequence:2,prizeName:"Novo prêmio",pattern:"FullCard"}])})});
