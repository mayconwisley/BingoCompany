import { useState } from "react";import { Link } from "react-router-dom";import { bingoApi } from "../features/bingo/bingoApi";import { useAsyncResource } from "../shared/hooks/useAsyncResource";import { AppShell } from "../shared/ui/AppShell";import { PageState } from "../shared/ui/PageState";
export function AdminPage(){const[name,setName]=useState("");const[mode,setMode]=useState("Automatic");const events=useAsyncResource(()=>bingoApi.listEvents(),[]);return <AppShell><main><header className="pagehead"><div><p className="eyebrow">Central de eventos</p><h1>Administração</h1></div></header><section className="panel formrow"><input aria-label="Nome do evento" placeholder="Festa de Confraternização 2026" value={name} onChange={e=>setName(e.target.value)}/><select aria-label="Modo de marcação" value={mode} onChange={e=>setMode(e.target.value)}><option value="Automatic">Marcação automática</option><option value="ManualRequired">Manual obrigatória</option><option value="AssistedManual">Manual assistida</option></select><button className="primary" disabled={!name.trim()} onClick={async()=>{await bingoApi.createEvent(name,mode);setName("");await events.reload()}}>Criar evento</button></section><PageState loading={events.loading} error={events.error}/><section className="grid">{events.data?.map(event=><article className="event" key={event.id}><span className="badge">{event.status}</span><h2>{event.name}</h2><p>Código público <strong>{event.publicCode}</strong></p><div className="actions"><Link className="button" to={`/admin/eventos/${event.id}?code=${event.publicCode}`}>Configurar</Link><Link className="button" to={`/display/${event.publicCode}`}>Abrir telão</Link></div></article>)}</section></main></AppShell>}
