import { useNavigate } from "react-router-dom";import { AppShell } from "../shared/ui/AppShell";
export function HomePage(){const navigate=useNavigate();return <AppShell><main className="hero"><p className="eyebrow">Bingo corporativo em tempo real</p><h1>A festa inteira<br/><em>joga junto.</em></h1><p>Cartelas digitais, sorteio auditável e uma experiência de telão feita para criar suspense.</p><div className="actions"><button className="primary" onClick={()=>navigate("/admin")}>Criar evento</button><button onClick={()=>{const code=prompt("Código do evento")?.trim();if(code)navigate(`/participar/${code}`)}}>Participar</button></div></main></AppShell>}
